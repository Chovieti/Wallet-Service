# 💰 Wallet Service

Тестовое задание: REST-сервис для управления балансом кошелька  
с поддержкой конкурентной нагрузки (до 1000 RPS на один кошелек).

---

## 📌 Технологический стек

- Java 17
- Spring Boot 3
- Spring Web
- Spring Data JPA
- PostgreSQL
- Liquibase
- Docker
- Docker Compose
- JUnit 5

---

## 🚀 Функциональность

### 1️⃣ Пополнение или списание средств

**Endpoint:** POST /api/v1/wallet

**Пример запроса:**
``` json
{
    "walletId": "22222222-2222-2222-2222-222222222222",
    "operationType": "DEPOSIT",
    "amount": 50.00
}
```
Параметры:

|     Поле      |    Тип     |                             Описание                             |
|:-------------:|:----------:|:----------------------------------------------------------------:|
|   walletId    |    UUID    |                     Индентификатор кошелька                      |
| operationType |   String   |             DEPOSIT(пополнение) или WITHDRAW(снятие)             |
|    amount     | BigDecimal | Сумма на которую измениться баланс (не может быть отрицательной) |

### 2️⃣⃣ Получение баланса кошелька

**Endpoint:** GET /api/v1/wallets/{walletId}

**Пример ответа:**
``` json
{
    "balance": 1000.00
}
```

### ⚙️  Обработка ошибок

Сервис возвращает корректные HTTP-коды с описанием ошибки

| Ситуация              | Код |
|-----------------------|:---:|
| Кошелек не найден     | 404 |
| Недостаточно средств  | 400 |
| Неверный тип операции | 400 |
| Невалидный JSON       | 400 |

**Формат ответа об ошибке:**

```
{
    "error": "Insufficient Funds",
    "message": "Insufficient funds in wallet ...: balance 1000.00, requested 50000.00",
    "timestamp": "2026-02-13T05:03:04.652667981"
}
```

Используется централизованная обработка через @RestControllerAdvice

### 🔒   Работа в конкурентной среде

Для работы сервиса при высокой нагрузке применены:
- Транзакции(@Transactional)
- Баланс изменяется одним 'UPDATE'-запросом
- Отсутствует read-modify-write цикл
- Конкурентность делегирована PostgreSQL

### 🗄️  База данных

- PostgreSQL
- Миграции реализованы через Liquidbase
- Файл миграции: src/main/resources/db/changelog/db.changelog-master.yaml
- Миграция выполняется автоматически при старте приложения

### 🐳   Запуск через Docker

**Сборка всей системы из корневой папки проекта**
```
docker compose build
```
Будут собраны:
- postgres
- wallet-app
- wallet-test

**Запуск проекта из корневой папки проекта**
```
docker compose up app
```
Будут запущены:
- postgres
- wallet-app

После запуска сервис доступен по адресу http://localhost:8080

### 🔧   Конфигурация
Настройка осуществляется через:
- .env
- переменные окружения
- application.properties

### 🧪 Эндпоинты покрыты тестами

Запуск тестов:
```
./mvnw test
или
docker compose up app-test
```

### 📂   Структура проекта
```
controller          - RES  API
controller/advice   - Глобальный обработчик ошибок
service             - Бизнес-логика
repository          - Доступ к данным
model               - Сущность Wallet
dto                 - DTO объекты
exception           - Кастомные исключение

db/changelog        - Liquidbase миграции
```